import { makeStyles } from '../../lib/utils/makeStyles'

const useStyles = makeStyles()(() => ({
  footer: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
  },
}))

export default useStyles
