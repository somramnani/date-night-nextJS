import { makeStyles } from '../lib/utils/makeStyles'

const useStyles = makeStyles()(() => ({
  materialIcons: {
    paddingTop: 20,
    paddingBottom: 0,
    fontSize: 75,
  },
  description: {
    paddingBottom: 10,
  },
}))

export default useStyles
